stages:
  - deploy

deploy_job:
  stage: deploy
  image: alpine:latest
  script:
    # 1. Install necessary tools
    - apk add --no-cache openssh-client rsync

    # 2. Setup the SSH directory and scan the VM's host key
    - mkdir -p ~/.ssh
    - chmod 700 ~/.ssh
    - ssh-keyscan -H "$VM_IP" >> ~/.ssh/known_hosts

    # 3. Fix permissions on the auto-generated key file
    # GitLab puts the path to the key in the $SSH_PRIVATE_KEY variable
    - chmod 400 "$SSH_PRIVATE_KEY"

    # 4. Deploy using rsync
    # We use -i with the variable directly as it points to the file path
    - rsync -avz -e "ssh -i $SSH_PRIVATE_KEY -o StrictHostKeyChecking=no" ./ "$VM_USER@$VM_IP:/home/$VM_USER/app/"
  
  only:
    - main